# Database Configuration (Required for PostgreSQL features)
DATABASE_URL=your_postgresql_connection_string_here

# Node Environment
NODE_ENV=production

# Session Configuration
SESSION_SECRET=your_secure_session_secret_here

# Optional: Port Configuration (Vercel handles this automatically)
PORT=5000